app:
  security:
    password:
      default-encoder: bcrypt
      bcrypt:
        cost-factor: 12
      scrypt:
        cpu-cost: 65536
        memory-cost: 8
        parallelism: 1
        key-length: 32
        salt-length: 16
      # ...